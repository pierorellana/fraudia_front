export const APP_ROUTES = {
  dashboard: '/dashboard',
  uploads: '/uploads',
  claims: '/claims',
  claimDetail: (id: string) => `/claims/${id}`,
  rules: '/rules',
  agent: '/agent',
  reports: '/reports',
};
